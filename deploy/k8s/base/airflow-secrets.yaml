# Template only — real values are injected by deploy script.
apiVersion: v1
kind: Secret
metadata:
  name: airflow-secrets
  namespace: antifraud-system
type: Opaque
stringData:
  AIRFLOW__CORE__FERNET_KEY: "REPLACE_ME"
  AIRFLOW_ADMIN_USERNAME: "REPLACE_ME"
  AIRFLOW_ADMIN_PASSWORD: "REPLACE_ME"
