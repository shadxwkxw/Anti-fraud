apiVersion: v1
kind: Service
metadata:
  name: antifraud-online
  namespace: antifraud-system
  labels:
    app: antifraud-online
spec:
  type: NodePort
  selector:
    app: antifraud-online
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
